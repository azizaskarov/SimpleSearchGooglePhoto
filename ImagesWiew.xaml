<Window x:Class="SimpleSearchGooglePhoto.ImagesWiew"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:SimpleSearchGooglePhoto"
        mc:Ignorable="d"
        Title="Image Search App" Height="730"  Width="1024" WindowStartupLocation="CenterScreen">
    <Grid>
        <TextBlock Name="SearchTextBox" Text="Images list " HorizontalAlignment="Left" VerticalAlignment="Top" Margin="10,10,0,0" Width="300"/>

        <ListBox Name="ImageListBox" Margin="10,50,-10,40"  
                 ScrollViewer.HorizontalScrollBarVisibility="Disabled">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <StackPanel Orientation="Horizontal">
                        <Image Width="150" Height="150" Source="{Binding}" Margin="5"/>
                    </StackPanel>
                </DataTemplate>
            </ListBox.ItemTemplate>
            <ListBox.ItemsPanel>
                <ItemsPanelTemplate>
                    <WrapPanel/>
                </ItemsPanelTemplate>
            </ListBox.ItemsPanel>
        </ListBox>

    </Grid>
</Window>
